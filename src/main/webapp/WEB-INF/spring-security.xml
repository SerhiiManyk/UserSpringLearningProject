<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <!-- 1. HTTP Security -->
    <http use-expressions="true"
          create-session="ifRequired">

        <!-- Публічні сторінки -->
        <intercept-url pattern="/login" access="permitAll"/>
        <intercept-url pattern="/newuser" access="permitAll"/>
        <intercept-url pattern="/registrationsuccess" access="permitAll"/>
        <intercept-url pattern="/successfull" access="permitAll"/>
        <intercept-url pattern="/resources/**" access="permitAll"/>

        <!-- Захищені сторінки -->
        <intercept-url pattern="/users/**" access="isAuthenticated()"/>

        <!-- Login -->
        <form-login
                login-page="/login"
                login-processing-url="/login"
                username-parameter="email"
                password-parameter="password"
                default-target-url="/users"
                authentication-failure-url="/login?error=true"/>

        <!-- Logout -->
        <logout
                logout-url="/logout"
                logout-success-url="/login?logout=true"
                invalidate-session="true"/>

        <!-- CSRF увімкнено за замовчуванням -->
    </http>

    <!-- 2. UserDetailsService -->
    <beans:bean id="customUserDetailsService" class="com.manser.pr.service.CustomUserDetailsService"/>

    <!-- 3. PasswordEncoder -->
    <beans:bean id="noopPasswordEncoder"
                class="org.springframework.security.crypto.password.NoOpPasswordEncoder"
                factory-method="getInstance"/>

    <!-- 4. Authentication manager з нашим сервісом -->
    <authentication-manager>
        <authentication-provider user-service-ref="customUserDetailsService">
            <password-encoder ref="noopPasswordEncoder"/>
        </authentication-provider>
    </authentication-manager>

</beans:beans>
