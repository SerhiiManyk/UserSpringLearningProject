<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-4.2.xsd">

    <!-- 1. Налаштування доступу -->
    <http auto-config="true" use-expressions="true">

        <!-- Доступ без логіну -->
        <intercept-url pattern="/login" access="permitAll" />
        <intercept-url pattern="/resources/**" access="permitAll" />

        <!-- Доступ лише для залогінених -->
        <intercept-url pattern="/users/**" access="isAuthenticated()" />

        <!-- Форма логіну -->
        <form-login
                login-page="/login"
                default-target-url="/users"
                authentication-failure-url="/login?error=true"
                username-parameter="email"
                password-parameter="password" />

        <!-- Logout -->
        <logout
                logout-url="/logout"
                logout-success-url="/login?logout=true"
                invalidate-session="true" />

    </http>

    <!-- 2. Тимчасові користувачі (in-memory) -->
    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="admin@mail.com"
                      password="{noop}admin"
                      authorities="ROLE_ADMINISTRATOR"/>

                <user name="user@mail.com"
                      password="{noop}user"
                      authorities="ROLE_REGULAR_USER"/>

                <user name="guest@mail.com"
                      password="{noop}guest"
                      authorities="ROLE_UN_LOGIN_USER"/>
            </user-service>
        </authentication-provider>
    </authentication-manager>

</beans:beans>